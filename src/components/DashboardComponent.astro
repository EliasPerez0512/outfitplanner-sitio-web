<!doctype html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Outfit Planner - Dashboard</title>
        <style>
            * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
            }
            :root {
                --bg-light: #e8ddcf;
                --bg-dark: #5c3d30;
                --text-light: #ffffff;
                --text-dark: #333333;
                --button-border: #5c3d30;
                --button-text-light: #e8ddcf;
            }

            body {
                font-family: -apple-system, BlinkMacSystemFont, "Segoe UI",
                    Roboto, sans-serif;
                background-image: linear-gradient(
                        rgba(0, 0, 0, 0.5),
                        rgba(0, 0, 0, 0.5)
                    ),
                    url("https://github.com/EliasPerez0512/Imagenes-Proyecto-DS9/blob/main/DashboardBackground.jpg?raw=true");
                min-height: 100vh;
                display: flex;
                flex-direction: column;
            }
            /* Header Styles */
            .header {
                background-color: var(--bg-light);
                padding: 20px 40px;
                border-bottom: 2px solid var(--bg-dark);
                display: flex;
                justify-content: space-between;
                align-items: center;
                height: 120px;
            }

            .logo-section {
                display: flex;
                align-items: center;
                gap: 12px;
                margin-left: 6%;
            }
            .logo-section img {
                width: 130px;
                height: 130px;
            }

            .profile-link {
                color: var(--bg-dark);
                text-decoration: none;
                font-size: 14px;
                font-weight: 500;
                margin-right: 6%;
            }

            .profile-link:hover {
                text-decoration: underline;
            }

            /* Body Styles */
            .main-content {
                flex: 1;
                justify-content: center;
                padding: 40px;
                max-width: 1200px;
                margin: 0 auto;
                margin-bottom: 50px;
            }

            .dashboard-title {
                font-size: 32px;
                font-weight: 600;
                color: var(--button-text-light);
                margin-bottom: 40px;
            }

            .dashboard-grid {
                display: grid;
                grid-template-columns: 1fr 1fr;
                gap: 24px;
                max-width: 800px;
            }

            .dashboard-card {
                background-color: var(--bg-dark);
                border-radius: 12px;
                padding: 32px;
                color: var(--bg-light);
                position: relative;
            }

            .card-title {
                font-size: 24px;
                font-weight: 600;
                margin-bottom: 20px;
                line-height: 1.3;
            }

            .card-number {
                font-size: 72px;
                font-weight: 700;
                line-height: 1;
            }

            .membership-card .card-subtitle {
                color: #d4af37;
                font-size: 28px;
                font-weight: 600;
                margin-bottom: 40px;
            }

            .details-button {
                background-color: var(--bg-light);
                color: var(--bg-dark);
                border: none;
                padding: 8px 16px;
                border-radius: 6px;
                font-size: 14px;
                font-weight: 500;
                cursor: pointer;
                position: absolute;
                bottom: 32px;
                right: 32px;
            }

            .details-button:hover {
                background-color: #4a3026;
                color: var(--bg-light);
            }

            .advice-card .card-text {
                font-size: 16px;
                line-height: 1.5;
                margin-top: 12px;
            }

            /* Footer Styles */
            .footer {
                background-color: var(--bg-dark);
                color: var(--bg-light);
                text-align: center;
                padding: 24px;
                font-size: 14px;
            }

            /* Responsive Design */
            @media (max-width: 768px) {
                .header {
                    padding: 16px 20px;
                }

                .main-content {
                    padding: 20px;
                }

                .dashboard-grid {
                    grid-template-columns: 1fr;
                    gap: 16px;
                }

                .dashboard-card {
                    padding: 24px;
                }

                .card-number {
                    font-size: 56px;
                }

                .dashboard-title {
                    font-size: 28px;
                    margin-bottom: 24px;
                }
            }
        </style>
    </head>
    <body>
        <!-- Header Section -->
        <header class="header">
            <div class="logo-section">
                <img
                    src="https://github.com/EliasPerez0512/Imagenes-Proyecto-DS9/blob/main/Logo.png?raw=true"
                    alt="Logo"
                />
            </div>
            <a href="#" class="profile-link">Perfil</a>
        </header>

        <!-- Body Section -->
        <main class="main-content">
            <h2 class="dashboard-title">Dashboard</h2>

            <div class="dashboard-grid">
                <div class="dashboard-card">
                    <h3 class="card-title">
                        Cantidad de prendas en el armario
                    </h3>
                    <div class="card-number">6</div>
                </div>

                <div class="dashboard-card">
                    <h3 class="card-title">Outfits creados</h3>
                    <div class="card-number">2</div>
                </div>

                <div class="dashboard-card membership-card">
                    <h3 class="card-title">Membresía:</h3>
                    <div class="card-subtitle">Premium</div>
                    <button class="details-button">Ver detalles</button>
                </div>

                <div class="dashboard-card advice-card">
                    <h3 class="card-title">Consejo de moda</h3>
                    <p class="card-text">
                        Los colores neutros como beige y gris combinan con todo.
                    </p>
                </div>
            </div>
        </main>

        <!-- Footer Section -->
        <footer class="footer">
            <p>
                &copy; Outfit Planner 2025 &middot; Todos los derechos
                reservados.
            </p>
        </footer>
    </body>
</html>
